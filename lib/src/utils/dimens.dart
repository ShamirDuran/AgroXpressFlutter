import 'package:flutter/material.dart';

// Used in borderRadius of inputs - forms
final kBorderRadius = BorderRadius.all(Radius.circular(20));

// Used in drawer dividers
final Color kDividerColor = Colors.grey;
final double kDividerHeight = 1.0;
